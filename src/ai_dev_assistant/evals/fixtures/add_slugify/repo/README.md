# textutils

Small text utilities.

## Functions

- `normalize_whitespace(text)` — collapse runs of whitespace to single spaces and strip the ends.
- `truncate(text, limit, ellipsis="…")` — cut text to at most `limit` characters, appending the ellipsis when cut.
